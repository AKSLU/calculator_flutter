# calculator_flutter
Простой калькулятор, созданный на **Flutter**.
Поддерживает базовые арифметические операции: сложение, вычитание, умножение, деление, сброс (AC) и ввод чисел.

Проект идеально подходит для обучения Flutter, работы с `StatefulWidget`, обработкой ввода и построением интерфейса.

---

## 📱 Функции калькулятора

* Ввод чисел от **0 до 9**
* Операции:

  * ➕ сложение
  * ➖ вычитание
  * ✖️ умножение
  * ➗ деление
* Кнопка **AC** — сброс всех данных
* Кнопка **=** — вычисление результата
* Простой и понятный пользовательский интерфейс
* Используется **Material Design**

---

## 🚀 Запуск проекта

Убедись, что у тебя установлен Flutter:

```bash
flutter --version
```

### 1. Клонировать репозиторий:

```bash
git clone https://github.com/AKSLU/calculator_flutter.git
```

### 2. Перейти в папку проекта:

```bash
cd calculator_flutter
```

### 3. Установить зависимости:

```bash
flutter pub get
```

### 4. Запустить приложение:

```bash
flutter run
```

---

## 📂 Структура проекта

Основной код расположен в:

```
lib/
 └── main.dart
```

---

## 🧱 Основные технологии

* **Flutter**
* **Dart**
* Работа с `StatefulWidget`
* Обработка нажатий кнопок через `GestureDetector`
* Обновление состояния с помощью `setState()`

---

## 🖼 Скриншоты (опционально)

Ты можешь добавить сюда изображения, например:

```
![Calculator Screenshot](screenshots/screen1.png)
```

---

## 💻 Код калькулятора (основные части)

Приложение запускается через:

```dart
runApp(const CalculatorApp());
```

Логика кнопок хранится в:

```dart
void buttonPressed(String value)
```

Кнопки создаются через:

```dart
Widget buildButton(String text)
```

---

